# MOMO Corn 功能开发任务提示词（提示词1）

> 适用对象：执行本任务的 AI Coding Agent
> 项目：MOMO Corn（双人情侣 App）
> 技术栈：Expo SDK ~56.0.11 / React Native 0.85.3 / React 19.2.3 / Supabase（数据库 + Storage + Realtime）/ 腾讯云 TIM
> 项目根目录：d:\APK\APK

---

## 〇、开始前的强制要求（必读）

1. **先读文档再写代码**：项目根目录 `AGENTS.md` 明确规定——Expo 已升级，写任何代码前必须阅读 https://docs.expo.dev/versions/v56.0.0/ 对应版本的官方文档，禁止使用旧版 API 凭记忆编写。
2. **先读代码再动手**：动手前必须通读以下关键文件，理解现有架构与代码风格，新代码必须与现有风格保持一致：
   - `App.js`（应用入口、登录、首页导航、所有 Screen 通过 `lazyScreen` 懒加载注册）
   - `src/theme/`（colors.js / typography.js / spacing.js / radius.js / shadows.js / layout.js / animations.js / index.js，现有设计令牌体系）
   - `src/screens/GomokuGameScreen.js`（五子棋主界面，约 6 万字节，功能三的直接改造对象）
   - `src/lib/gomokuUtils.js`、`src/lib/gomoku_schema.sql`（五子棋数据层）
   - `src/lib/realtimeSignal.js`、`src/lib/drawGuessSignalQueue.js`、`src/lib/drawGuessSync.js`（现有双人实时同步方案，可复用范式）
   - `src/lib/imageCache.js`、`src/lib/photoUtils.js`、`src/lib/imageSaver.js`、`src/lib/mediaResolver.js`（现有图片上传/缓存/解析方案，厨房模块必须复用）
   - `src/lib/lazyScreen.js`、`src/lib/fetchWithTimeout.js`、`src/lib/wakeUpSupabase.js`（现有性能优化基建）
   - `src/components/ui/`（现有 UI 组件库，优先复用 Button、AppInput 等）
   - `提示词/UI/`（历史 UI 参考稿，如有）
3. **安全红线**：
   - 严禁在代码或提交中硬编码任何密钥、Service Role Key、Token；所有敏感配置只从现有配置文件读取。
   - 新增 Supabase 表必须启用 RLS（行级安全），策略限定为"当前情侣双方"可读写，参照 `src/lib/gomoku_schema.sql`、`src/lib/drawGuess_schema.sql` 的写法。
4. **不破坏既有功能**：本任务全部为增量开发与优化，任何已有功能（聊天、打卡、纪念日、时间胶囊、旅行日记、语音信箱、心愿清单、你画我猜、阅后即焚、五子棋）不得出现回归。
5. **完成标准**：每个功能完成后必须通过 `npm run lint` 与 `npm test`，并人工走查验收清单。注释使用简洁中文，与现有代码注释风格一致。

---

## 一、功能一：新增"momi厨房"做饭模块

### 1.1 功能概述

为 App 新增一个双人协同的做饭模块，命名为 **"momi厨房"**。双方用户可以共同维护一份菜品库（上传、编辑菜品与食谱），在点菜界面按分类浏览菜品，并可把本周想吃的菜加入"本周清单"。

### 1.2 入口与导航

- 在 App 首页功能入口区新增"momi厨房"入口卡片/图标（图标建议使用 Ionicons 的 `restaurant` 或 `fast-food` 系列，风格与现有入口一致）。
- 在 `App.js` 中通过 `lazyScreen(() => require('./src/screens/MomiKitchenScreen'))` 方式懒加载注册，**不得增加冷启动负担**。
- 模块内部包含三个层级页面（可用状态机或内部导航实现，保持与现有单 Activity 架构一致）：
  1. **点菜界面**（菜品列表，默认页）
  2. **菜品详情页**（查看/编辑食谱）
  3. **本周清单页**（本周想吃）

### 1.3 点菜界面（菜品列表页）

- **分类 Tab**：页面顶部为分类切换栏，固定三个分类：**荤菜 / 蔬菜 / 小吃**。默认选中"荤菜"。Tab 切换需有平滑的指示器滑动动画。
- **菜品卡片**：每个分类下以**两列网格卡片**展示菜品，卡片由 **菜品图片 + 菜品标题** 组成：
  - 图片占卡片上部约 70%，圆角（参照 `src/theme/radius.js`），居中裁剪（`contentFit="cover"`），加载时显示骨架屏或柔和占位色块；
  - 标题位于图片下方，使用 `typography` 中标题字号，超出一行省略；
  - 卡片带柔和阴影（参照 `shadows.js`），点击时有缩放反馈（如 0.97 缩放 + 回弹，可参照 `animations.js`）。
- **空状态**：某分类暂无菜品时，显示温馨插画占位（可用现有 assets 或简洁的图标+文案，如"还没有菜品，快来上传第一道吧~"），并提供明显的"上传菜品"按钮。
- **上传入口**：列表页右上角或右下角悬浮按钮（FAB）提供"上传菜品"入口。
- **数据同步**：菜品库为双方共享，任一方新增/编辑/删除后，另一方界面应通过 Supabase Realtime 自动刷新（复用 `realtimeSignal.js` 的订阅范式），无需手动下拉。

### 1.4 上传 / 编辑菜品

- 点击"上传菜品"后弹出表单（Modal 或新页面），字段包括：
  - **菜品图片**：调用 `expo-image-picker` 从相册选择或拍照，上传前用 `expo-image-manipulator` 压缩（复用 `photoUtils.js` 的压缩逻辑），上传至 Supabase Storage 专用 bucket（建议命名 `kitchen-images`），图片路径存入数据库，展示时走 `imageCache.js` 缓存。
  - **菜品标题**：必填，限制 20 字以内。
  - **所属分类**：荤菜 / 蔬菜 / 小吃 三选一，必选。
  - **食谱**（可留空，详情页中再补充）：见 1.5。
- **编辑与删除**：双方均可编辑任何菜品的图片、标题、分类；菜品卡片长按或详情页提供编辑入口；删除需二次确认弹窗。编辑/删除同样实时同步给对方。
- **权限**：仅限当前情侣双方账号可读写（RLS 保障）。

### 1.5 菜品详情页（食谱）

- 点击菜品卡片进入详情页，页面结构自上而下：
  1. **菜品大图**（顶部沉浸式大图，支持手势返回）；
  2. **菜品标题 + 分类标签**；
  3. **"本周想吃"按钮**：见 1.6；
  4. **食谱区域**：食谱支持两种形式，可并存：
     - **文字食谱**：多行文本（建议分"用料"与"步骤"两节，或自由文本），双方均可点击"编辑"进入编辑模式，保存后实时同步给对方；
     - **图片食谱**：支持上传一张或多张食谱图片（如手写菜谱拍照），图片可放大查看、可删除、可继续追加。
- 食谱内容为空时显示引导文案："还没有食谱，快来写下第一道菜的做法吧~"。

### 1.6 本周想吃（清单功能）

- 在菜品详情页（或菜品卡片上的快捷入口）提供 **"本周想吃 / 加入清单"** 操作：
  - 点击后该菜品进入本周清单，按钮状态变为"已加入清单"（视觉区分：如填充色变为主色、图标变为对勾）；
  - 再次点击可取消加入。
- **交互反馈（必须精确实现）**：点击"加入清单"成功的瞬间，**屏幕中央弹出提示，文案一字不差为：`momi厨房正在准备食材，请耐心等待哦~`**。提示样式要求：居中浮层 Toast，圆角卡片样式，带轻微缩放弹入动画，约 1.5~2 秒后自动淡出，不阻塞后续操作。
- **本周清单页**：点菜界面提供入口（如右上角"清单"图标，带数量角标）进入本周清单页：
  - 按"荤菜 / 蔬菜 / 小吃"分组展示本周已选菜品（小图 + 标题列表样式即可）；
  - 支持单条移除、一键清空（需二次确认）；
  - 清单以"自然周"为周期（周一为一周起点），新的一周自动清空上一轮清单（以 `week_start` 字段标记归属周，展示时只显示当前周的记录，历史记录保留不删除）；
  - 双方共享同一份清单，任一方增删实时同步。

### 1.7 数据模型建议（Supabase）

新建 SQL 迁移文件 `src/lib/kitchen_schema.sql`（放项目内并在交付说明中提示需到 Supabase SQL Editor 执行），参考现有 `gomoku_schema.sql` 风格，至少包含：

- `kitchen_dishes` 表：`id`（uuid 主键）、`couple_id`（情侣标识，沿用现有表的情侣关联字段约定）、`category`（text，枚举：`meat`/`vegetable`/`snack`）、`title`（text）、`image_path`（text，Storage 路径）、`recipe_text`（text，可空）、`recipe_images`（jsonb 数组，可空）、`created_by`（创建者标识）、`created_at`、`updated_at`。
- `kitchen_weekly_picks` 表：`id`、`couple_id`、`dish_id`（外键关联 kitchen_dishes，级联删除）、`picked_by`、`week_start`（date，本周周一日期）、`created_at`，并对 `(couple_id, dish_id, week_start)` 建唯一约束防重复加入。
- Storage bucket `kitchen-images` 及对应 RLS 策略：仅情侣双方可读、可写。
- 两张表均启用 RLS 并开启 Realtime 发布（`supabase_realtime` publication）。

### 1.8 UI/UX 设计要求

- **整体风格**：精美美观、现代化、简约且温馨。延续 App 现有薰衣草紫主色调与设计令牌体系（`src/theme/`），厨房模块可适当融入暖色系点缀（如奶油色、暖橙）营造"厨房的温度"，但不得与全局风格割裂。
- **参考对象**：界面布局与交互可参考下厨房、懒饭、小红书收藏夹等成熟产品的卡片流设计。
- **互动效果**：卡片按压反馈、Tab 指示器滑动、图片加载渐入、Toast 弹入淡出、按钮状态切换动画等微交互必须自然流畅（60fps，使用 `Animated` 或 Reanimated 类方案，禁止 layout 动画掉帧）。
- **安全区适配**：使用 `react-native-safe-area-context` 处理刘海屏与底部手势区。
- 图片统一使用 `expo-image` 渲染并接入 `imageCache.js` 缓存策略。

---

## 二、功能二：多主题切换系统

### 2.1 功能目标

将当前 `src/theme/colors.js` 的**静态单套配色**升级为**多套清新风格主题可切换**的主题系统，切换后全 App 所有界面即时生效、风格一致。

### 2.2 技术实现要求

- **主题定义**：在 `src/theme/` 下新增主题注册文件（如 `themes.js`），每个主题提供与现有 `colors.js` **完全相同的语义化令牌结构**（primary、mint、coral、amber、neutral、background、surface、textPrimary、primaryAction、me、partner、success、error 等全部令牌一个不缺），保证业务代码零改动即可换肤。
- **主题清单**：至少提供 4 套清新风格主题（名称可调整，但每套需给出完整配色）：
  1. **薰衣草物语**（现有默认配色，保持不变）；
  2. **薄荷清晨**（薄荷绿系，清新通透）；
  3. **蜜桃汽水**（粉橘系，甜美温馨）；
  4. **晴空微风**（天空蓝系，干净明朗）。
  每套主题需保证：文字与背景对比度满足可读性（正文对比度 ≥ 4.5:1）、`me`/`partner` 双方身份色区分明显、按压态/禁用态/软底色（Soft 变体）成套提供。
- **主题上下文**：新增 `ThemeProvider` + `useTheme()` Hook（可放 `src/theme/index.js` 或新增 `src/hooks/useTheme.js`），在 `App.js` 最外层包裹；业务组件从 `useTheme()` 取色。**迁移策略**：优先保证所有界面生效；对存量 `import { colors } from './src/theme'` 的静态引用，可采用"colors 变为动态 getter/代理"或"批量替换为 useTheme"的方式，二选一并在交付说明中说明理由，确保**不遗漏任何界面**。
- **持久化**：用户选择的主题保存至 `@react-native-async-storage/async-storage`（key 如 `momo.theme.id`），App 启动时在首帧渲染前读取并应用，**避免启动时先显示默认色再跳变的闪烁问题**（可在 `restoreSession` 阶段同步预取）。
- **切换入口**：在首页设置区/个人区新增"主题换装"入口，进入主题选择页：以卡片形式展示每套主题的色板预览（主色块 + 示例界面缩略示意），点击即选即生效，当前主题有选中态标识。
- **平滑过渡**：切换主题时全界面颜色应平滑过渡（对背景色等大面积颜色可加 200~300ms 的过渡动画），**禁止出现白屏、闪烁、卡顿**；`StatusBar` 的 `barStyle` 与背景色随主题联动更新。
- **双人同步（加分项，可选）**：如实现成本低，可通过 Supabase 将主题选择同步给对方，实现"情侣同款主题"；若不做需在交付说明中明确说明。

### 2.3 验收要点

- 切换主题后逐屏检查：登录页、首页、聊天、打卡、纪念日、时间胶囊、旅行日记、语音信箱、心愿清单、你画我猜、阅后即焚、五子棋、momi厨房——全部生效且无遗漏色块。
- 杀掉 App 重进，主题保持。
- 切换过程无闪烁、无掉帧。

---

## 三、功能三：五子棋新增"上一步"悔棋功能

### 3.1 改造对象

`src/screens/GomokuGameScreen.js` + `src/lib/gomokuUtils.js` + （如涉及数据结构）`src/lib/gomoku_schema.sql`。先通读现有实现，明确当前是"双人实时对战"（经 Supabase/TIM 同步）还是含本地模式，悔棋逻辑必须与现有同步机制兼容。

### 3.2 功能要求

- **入口**：在五子棋游戏操作区新增按钮，按钮文案固定为 **"上一步"**（样式与现有操作按钮一致，可配 `arrow-undo` 图标）。
- **悔棋规则**：
  - 点击"上一步"后，撤销**最近一手棋**（棋盘上移除该子，回合权回退到上一手落子方）；
  - **双人联网对战时**：悔棋属于影响对方的数据变更——采用**申请-确认制**：发起方点击"上一步"后，对方弹出确认提示（如"对方请求悔棋，同意吗？"），对方同意后双方棋盘同步回退；对方拒绝或 15 秒未响应则视为不生效，并给发起方轻提示。若现有架构实现确认制成本过高，可降级为"直接悔棋并实时同步给对方"，但必须在交付说明中写明取舍理由；
  - **本地/单机模式**（如存在）：直接悔棋，无需确认；
  - 每方每局可悔棋次数建议不限，但**游戏已分出胜负后禁止悔棋**；
  - 无棋可悔（棋盘为空或尚未轮到可悔状态）时点击"上一步"，按钮置灰或给出轻提示。
- **交互提示（必须精确实现）**：悔棋成功生效后，**屏幕中央弹出提示，文案一字不差为：`下一次不要点错了哦~`**。样式要求与功能一的 Toast 一致（居中圆角浮层、弹入动画、1.5~2 秒自动消失）。
- **数据一致性**：
  - 悔棋后的棋盘状态、回合方、步数必须双方一致，以服务端/数据库记录为准，禁止出现双方棋盘不一致；
  - 如现有实现将棋步落库（`gomoku_schema.sql`），悔棋需同步删除/标记对应棋步记录，刷新、断线重连后恢复的棋盘必须是悔棋后的正确状态；
  - 悔棋操作与落子操作共用同一套同步通道与防并发机制（如 `drawGuessSignalQueue.js` 式的队列），快速连续点击不得导致状态错乱（按钮需防抖/防连点）。

---

## 四、功能四：性能优化（启动提速 + 五子棋降延迟）

### 4.1 优化前必须先测量（强制）

先建立性能基线，再优化，最后对比。**没有数据的优化视为未完成**：

- **冷启动耗时**：在 Android 真机 release 包上测量"点击图标 → 首页可交互"的耗时（可用 `performance.now()` 在 `index.js`/App 首帧与首页 `onLayout` 打点，或 `adb shell am start -W` 的 TotalTime），连续测 5 次取平均；
- **五子棋延迟**：在双人对战中测量"一方落子 → 另一方棋盘更新"的端到端耗时（在落子发送与接收回调处打时间戳日志），连续测 20 手取平均与 P95；同时记录当前同步链路（Supabase Realtime？轮询？TIM？）作为归因依据。

### 4.2 启动加载优化（解决偶发进入慢）

在**不破坏现有懒加载架构**（`lazyScreen`）的前提下排查并优化，方向包括但不限于：

1. **启动任务梳理**：梳理 `App.js`/`index.js` 首帧前的全部同步工作（polyfill、restoreSession、TIM 初始化、`wakeUpSupabase`、AsyncStorage 读取等），将非首帧必需的任务（TIM 登录、Supabase 保活唤醒、非首屏数据预取）延后到首帧渲染完成后执行（`InteractionManager.runAfterInteractions` 或 `requestIdleCallback` 式调度）；
2. **首屏渲染提速**：首页首帧只渲染可见内容，长列表/卡片流分页或虚拟化（`FlatList` + `windowSize`/`initialNumToRender` 调优）；检查首屏是否存在同步的重计算、大 JSON 解析、未压缩图片解码；
3. **图片与资源**：全 App 图片走 `expo-image` + `imageCache.js` 磁盘缓存，启动阶段预取首屏关键图；检查 assets 中是否有超大图（如 `pic.jpg` 这类大图不得打进首屏）；
4. **网络冷启动**：针对 Supabase 免费层冷启动慢的问题，优化 `wakeUpSupabase.js` 的唤醒时机（提前到登录页展示即开始并行唤醒）与超时策略（复用 `fetchWithTimeout.js`），避免首屏请求排队等唤醒；
5. **包体与 JS 执行**：确认 release 构建启用 Hermes；移除 release 中的 `console.log`（babel 插件或 Metro 配置）；用 `npx expo-doctor` 与 bundle 分析排查异常依赖；
6. **稳定性**：优化后不得引入白屏、闪屏、登录态闪烁等回归。

### 4.3 五子棋双人延迟优化

先定位瓶颈（信令通道延迟 / 数据库写入再订阅的往返 / 载荷过大 / 重连抖动），再针对性优化，方向包括但不限于：

1. **同步链路**：若当前为"写库 → 对方 Realtime 订阅收到变更"的链路，评估改为/补充**低延迟直连信令**（参考 `realtimeSignal.js` 的 broadcast/presence 或 `drawGuessSignalQueue.js` 的队列方案），落子瞬间先发轻量信令让对方即时渲染，数据库写入作为兜底与断线恢复依据；
2. **载荷瘦身**：落子消息只传最小字段（x、y、颜色、序号、时间戳），已有 `pako` 依赖可用于压缩大消息（如整盘同步时）；
3. **乐观渲染**：本方落子立即上屏（不等服务端回包），失败再回滚提示；
4. **弱网与重连**：断线自动重连并通过服务端棋步记录增量拉平状态；增加心跳与延迟指示（可选：界面显示实时 ping 值）；
5. **防抖防重**：落子、悔棋信令均需去重与顺序保证（序号/队列），杜绝快网双击、重连重放导致的棋盘错乱。

### 4.4 优化目标与交付数据

- 冷启动到可交互：相对基线**提升 ≥ 30%**（或绝对值 ≤ 2.5 秒，取其先达者），并消除"偶发卡很久"的长尾（P95 明显收敛）；
- 五子棋落子端到端延迟：平均 **≤ 500ms**、P95 **≤ 1s**（正常家庭 Wi-Fi/4G 环境）；
- 交付说明中必须附**优化前后对比表**：测量环境（机型、系统、网络）、测量方法、基线数据、优化后数据、每项优化点对应的贡献说明。

---

## 五、总体验收清单（DoD）

逐项自检，全部通过才算完成：

**momi厨房**
- [ ] 首页有"momi厨房"入口，模块懒加载，不影响冷启动
- [ ] 点菜界面含 荤菜/蔬菜/小吃 三个分类 Tab，卡片为"图片+标题"两列网格
- [ ] 双方均可上传菜品（图片压缩上传 Storage）、编辑、删除，且实时同步对方
- [ ] 详情页食谱支持文字编辑与图片上传两种形式，双方可编辑，实时同步
- [ ] "加入清单"后屏幕中央精确弹出"momi厨房正在准备食材，请耐心等待哦~"，自动消失
- [ ] 本周清单页可查看/移除/清空，按自然周归属，双方实时同步
- [ ] RLS 生效：非本情侣账号无法读写 kitchen 数据与图片
- [ ] UI 精美温馨、微交互流畅、适配安全区

**主题切换**
- [ ] ≥ 4 套清新主题可选，令牌结构完整，全 App 所有界面生效无遗漏
- [ ] 主题选择持久化，重启保持；启动无主题跳变闪烁
- [ ] 切换过程平滑无闪烁卡顿，StatusBar 联动

**五子棋悔棋**
- [ ] "上一步"按钮可用，悔棋后双方棋盘状态一致
- [ ] 联网对战按方案实现申请-确认（或文档说明的降级方案）
- [ ] 悔棋成功后精确弹出"下一次不要点错了哦~"
- [ ] 胜负已分禁止悔棋；刷新/重连后状态正确；快速连点不出错

**性能**
- [ ] 附优化前后启动耗时、五子棋延迟对比数据表（含测量方法与设备信息）
- [ ] 启动提速达成 4.4 目标；五子棋延迟达成 4.4 目标
- [ ] `npm run lint`、`npm test`、`npx expo-doctor` 全部通过

**全局**
- [ ] 无任何既有功能回归（逐屏走查）
- [ ] 无密钥硬编码；新增 SQL 已整理为迁移文件并附执行说明
- [ ] 代码风格、中文注释风格与现有代码一致

---

## 六、交付物

1. 全部源码改动（含新增的 `MomiKitchenScreen` 相关文件、`kitchen_schema.sql`、主题系统文件、五子棋与性能优化改动）；
2. `src/lib/kitchen_schema.sql` 迁移文件及执行说明（需用户到 Supabase SQL Editor 执行的步骤清单）；
3. 交付说明（追加到 `HANDOVER.md` 或单独说明）：实现方案要点、取舍说明（如悔棋确认制/降级、主题迁移策略、延迟链路方案）、优化前后性能对比表、遗留事项。

> 工作方式要求：尽量自动完成全部操作（装依赖、改代码、跑 lint/测试），只有确实需要人工的环节（如 Supabase SQL 执行、真机实测）才列出清晰步骤交给用户。
